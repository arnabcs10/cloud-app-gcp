# =============================================================================
# Observability Secrets
# Update the stringData values before deploying, or manage via ExternalSecrets.
# =============================================================================
---
# Grafana admin credentials + SMTP password
apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin-secret
  namespace: ns-observability
  labels:
    app: grafana
    app.kubernetes.io/part-of: observability-stack
type: Opaque
stringData:
  # TODO: Replace before deploying
  admin-user: "admin"
  admin-password: "REPLACE_WITH_STRONG_PASSWORD"
  smtp-password: "REPLACE_WITH_SMTP_APP_PASSWORD"
---
# Alertmanager SMTP password
apiVersion: v1
kind: Secret
metadata:
  name: alertmanager-smtp-secret
  namespace: ns-observability
  labels:
    app: alertmanager
    app.kubernetes.io/part-of: observability-stack
type: Opaque
stringData:
  # TODO: Replace with Gmail App Password or equivalent
  smtp-password: "REPLACE_WITH_SMTP_APP_PASSWORD"
